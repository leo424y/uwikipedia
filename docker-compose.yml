version: '3'
services:
  webserver:
    build: ./nginx
    ports:
      - "80:80"
    links:
      - web:web
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/app
    ports:
      - "3000"
